#include "header.hpp"

using core::check;

/* -----------------------------------------------------------------------------
----------------------------------------------------------------------------- */
DC();

int main (int argc, char *argv[]) {
  DI(std::shared_ptr<core::debug::Stream> errs(new core::debug::Stream());)
  DOPEN(, errs);
  io::file::DOPEN(, errs);
  io::socket::DOPEN(, errs);

  return 0;
}

/* -----------------------------------------------------------------------------
----------------------------------------------------------------------------- */
